name: Build signed APK & AAB

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - claude/new-session-9h26z5

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build unsigned release APK and AAB
        run: ./gradlew assembleRelease bundleRelease

      - name: Sign APK
        id: sign_apk
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          # The action defaults to build-tools 29.0.3, which no longer exists
          # on ubuntu-latest runners. Pin to a version the runner image ships.
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: Sign AAB
        id: sign_aab
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/bundle/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: focusdrift-release-apk
          path: ${{ steps.sign_apk.outputs.signedReleaseFile }}

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: focusdrift-release-aab
          path: ${{ steps.sign_aab.outputs.signedReleaseFile }}
